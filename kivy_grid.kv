GridLayout:
    cols: 3
    Button:
        text:"x"
        font_size: 100
        on_press: app.pressed(self)
        size_hint_x: None
        width: 100
    Button:
        text:"o"
        font_size: 100
        on_press: app.pressed(self)
    Button:
        text:"x"
        font_size: 100
        on_press: app.pressed(self)
    Button:
        text:"o"
        font_size: 100
        on_press: app.pressed(self)
    Button:
        text:"x"
        font_size: 100
        on_press: app.pressed(self)
    Button:
        text:"o"
        font_size: 100
        on_press: app.pressed(self)
    Button:
        text:"o"
        font_size: 100
        on_press: app.pressed(self)
    Button:
        text:"o"
        font_size: 100
        on_press: app.pressed(self)
    Button:
        text:"x"
        font_size: 100
        on_press: app.pressed(self)
    Label:
        text:"who cares about this stupid game"

